import * as React from 'react';
import Button from '@mui/material/Button';

export default function MyShinyButton() {
  return (
    <div>
      <Button variant="contained">Hello World</Button>
    </div>
  );
}
